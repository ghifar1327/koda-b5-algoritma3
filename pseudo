MENHITUNG DISKON BELANJA

DECLARE subtotal : INTEGER
DECLARE diskon10, diskon5 : DOUBLE

BEGIN
INPUT (subtotal)
 IF subtotal > 1000000
  diskon10 <- subtotal * (10 / 100)
  OUTPUT(diskon10)
 ELSEIF subtotal >= 500000
  diskon5 <- subtotal * (5 / 100)
  OUTPUT(diskon5)
 ENDIF
  OUTPUT(subtotal)
END
